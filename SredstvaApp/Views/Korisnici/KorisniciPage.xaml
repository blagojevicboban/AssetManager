<Page x:Class="SredstvaApp.Views.Korisnici.KorisniciPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Korisnici">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Korisnički nalozi" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Upravljanje pristupom i ulogama zaposlenih" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            
            <Button DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Content="Novi korisnik" Style="{StaticResource PrimaryButton}" 
                    Width="140" Height="36" Click="BtnNoviKorisnik_Click"/>
        </DockPanel>

        <!-- Search / Filter (Optional) -->
        <Border Grid.Row="1" Background="#0AFFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Pretraga:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0"/>
                <TextBox x:Name="TxtPretraga" Width="200" Height="32" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretraga_TextChanged"/>
            </StackPanel>
        </Border>

        <!-- Grid -->
        <DataGrid x:Name="DgKorisnici" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ime i prezime" Binding="{Binding ImePrezime}" Width="250"/>
                <DataGridTextColumn Header="Korisničko ime" Binding="{Binding KorisnickoIme}" Width="150"/>
                <DataGridTextColumn Header="Uloga" Binding="{Binding Uloga}" Width="150"/>
                
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding JeAktivan, Converter={StaticResource BooleanToStatusColorConverter}}" 
                                    CornerRadius="4" Padding="6,2" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding JeAktivan, Converter={StaticResource BooleanToStatusTextConverter}}" 
                                           Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Akcije" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Izmeni" Style="{StaticResource SecondaryButton}" Tag="{Binding Id}" Click="BtnIzmeni_Click" Margin="0,0,8,0"/>
                                <Button Content="Poništi lozinku" Style="{StaticResource SecondaryButton}" Tag="{Binding Id}" Click="BtnPonistiLozinku_Click" Margin="0,0,8,0"/>
                                <Button Content="{Binding JeAktivan, Converter={StaticResource BooleanToDeactivateTextConverter}}" 
                                        Style="{StaticResource SecondaryButton}" Tag="{Binding Id}" Click="BtnDeaktiviraj_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
